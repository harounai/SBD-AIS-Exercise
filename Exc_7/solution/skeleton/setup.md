# todo note commands
 
 ```
 docker swarm init
docker stack deploy -c docker-compose.yml
docker swarm join-token manager
docker stack deploy -c docker-compose.yml
docker service ls
docker service ps sbd3_frontend --no-trunc
docker node ls
 ```